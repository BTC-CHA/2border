name: CAD ASSY Prefix Once

on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/oneoff/cad-assy-prefix.py'
      - '.github/workflows/cad-assy-prefix-once.yml'
  workflow_dispatch:

jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Run patch
        run: python3 scripts/oneoff/cad-assy-prefix.py
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/verifyx/teacher/question-bank/page.js
          git commit -m "fix: use ASSY prefix for CAD assembly questions"
          git push
